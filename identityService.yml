eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"      
user:
  name: Venkat