---
spring:
  application:
    name: service-registry
  config:
    activate:
      on-profile: master
server:
  port: 8761     
eureka:      
  client:
    register-with-eureka: false
    fetch-registry: false
...
   
